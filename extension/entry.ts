import type ExtConfig from "@structs/ext_config";

const config: ExtConfig = {
    id: "helloworld",
    name: "Test Extension",
    description: "",
    version: "1.0.0",
    platform: ["gandi", "tw"],
    author: "KOSHINO",
    language: "en"
};

export default config;
